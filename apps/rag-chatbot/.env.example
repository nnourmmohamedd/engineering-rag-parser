# Copy to .env.local (git-ignored) and edit as needed. No secrets belong
# here -- this application ships no authentication.

# Backend origin the Vite dev server proxies /api requests to.
VITE_API_TARGET=http://127.0.0.1:8000
