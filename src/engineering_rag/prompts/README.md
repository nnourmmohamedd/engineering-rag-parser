# `prompts` — future LLM prompt boundary, not implemented

This is where future query-rewriting, retrieval, answer-generation and
evaluation prompt templates will live, once a generation stage exists.

The current deterministic parser needs no LLM prompts and has none — no
fake or unused prompt string is added here ahead of that milestone.
