"""User/system entry points.

Today: the ``engrag-parse`` Typer CLI (:mod:`engineering_rag.api.cli`). A
future HTTP interface (FastAPI) belongs in this package too, alongside the
CLI — not inside any service. See ``README.md`` in this directory.
"""

from __future__ import annotations
